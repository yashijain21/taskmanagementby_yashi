# Task Manager Frontend

## Setup

1. Install dependencies:
   - `npm install`
2. Copy env file:
   - `copy .env.example .env.local`
3. Run app:
   - `npm run dev`

## Pages

- `/register`
- `/login`
- `/dashboard`
